{
  "name": "portfolio-ernest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "npm install",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
